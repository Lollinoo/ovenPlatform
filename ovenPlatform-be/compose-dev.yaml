services:
  oven-backend:
    image: oven-backend-dev:0.0.1
    container_name: oven-backend-dev
    ports:
      - "3001:3000"
    volumes:
      - ./:/app
      - ./node_modules:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  my-node-modules: